# Engine service dependencies (feature + signal + AI)
-r base.txt

# Technical analysis
# Built-in indicators live in src/algo/indicators.py.
# TA-Lib and pandas-ta are intentionally excluded from the default install
# because the current codebase does not import them and they break Python 3.13 installs.

# Statistics
scipy>=1.14,<2
statsmodels>=0.14.1,<1
scikit-learn>=1.3.2,<2

# OpenAI for GPT validation & AI advisor
openai>=1.6,<3
tiktoken>=0.5,<1

# Feature engineering
# feature-engine removed — not imported; using src/engines/feature_engine.py

# ML trade learning
joblib>=1.3,<2
