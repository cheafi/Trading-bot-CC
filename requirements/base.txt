# Base dependencies for all services
# Python 3.11-3.13

# Database
asyncpg>=0.29,<1
psycopg2-binary>=2.9,<3
sqlalchemy[asyncio]>=2.0.23,<3
alembic>=1.13,<2
greenlet>=3.1,<4

# Redis
redis>=5,<8
# aioredis removed — use redis.asyncio instead

# HTTP clients
httpx>=0.26,<1
aiohttp>=3.9,<4
requests>=2.31,<3

# Data processing
pandas>=2.2,<4
numpy>=1.26,<3
# polars removed — not imported anywhere

# Configuration
pydantic>=2.5,<3
pydantic-settings>=2.1,<3
python-dotenv>=1.0,<2
pyyaml>=6.0,<7

# Logging & monitoring
structlog>=23.3,<26
python-json-logger>=2.0,<5
prometheus-client>=0.19,<1

# Utilities
python-dateutil>=2.8,<3
pytz>=2023.3
tenacity>=8.2,<10  # retry logic

# Azure (for Azure OpenAI authentication)
azure-identity>=1.15,<2

# Broker APIs (optional - install as needed)
# pip install futu-api  # For Futu (富途)
# pip install ib_insync  # For Interactive Brokers
# pip install MetaTrader5  # For MetaTrader 5 (Windows only)
